# @sweepnflip/widgets

Prebuilt UI kit for embedding Sweep n' Flip NFT AMM swap flows with minimal integration
work — lands in **Phase 56**.

This directory is an intentional placeholder so the `snf-sdk` workspace shape is final
from Phase 54 onward. It has no source, no build step, and no publish configuration yet
(`"private": true`, version `0.0.0`). Do not add components here before Phase 56.
