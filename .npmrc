# Peers (viem/wagmi/react/@tanstack/react-query) are declared deliberately in
# peerDependencies for both published packages and must NOT be auto-installed
# into consumers' node_modules or silently pulled in during local installs.
auto-install-peers=false
strict-peer-dependencies=false
link-workspace-packages=true
