# Own pnpm workspace root. Also isolates this repo if it happens to be checked out
# nested inside a larger directory tree that has its own pnpm-workspace.yaml further
# up (pnpm walks up the tree otherwise).
packages:
  - packages/*
  - examples/*
